# 보안 정책상 외부 LLM API를 사용하지 않는다 (검색 결과만 반환하는 단순 검색 기능).
# 관련 API 키 설정은 존재하지 않음.

# 검색 시 기본으로 가져올 청크 개수
TOP_K_RESULTS=5

# CORS 허용 프론트엔드 주소
FRONTEND_ORIGIN=http://localhost:3000

# 타지사 고장사례 원본 폴더 (기본값: backend/fault_cases). 다른 위치를 쓰려면 주석 해제.
# FAULT_CASE_DIR=C:\path\to\fault_cases
